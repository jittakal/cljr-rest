(ns ^{:author "Jitendra Takalkar"
      :doc "Test Core Database"}
  cljr-rest.db.core-test
  (:use clojure.test
        cljr-rest.db.core))

;;(deftest test-drop-tables
;;  (testing "Drop tables"
;;    (is (= '(0) (drop-blogs)))
;;    (is (= '(0) (drop-users)))))

;;(deftest test-create-tables
;;  (testing "Create tables"
;;    (is (= '(0) (create-blogs)))
;;    (is (= '(0) (create-users)))))


